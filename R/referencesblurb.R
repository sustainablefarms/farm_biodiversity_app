referencesblurb <- tags$html(tags$p("Often it is useful to compare the current set of estimates to estimates under different scenarios.",
				   "The reference estimates are useful for doing this, and can be set using the 'Update' button and 'Use Default' checkbox in the bottom right of the app.",
				   "After you set the current estimates as the 'reference', you can alter your rainfall, patches and location and watch how the estimates change."),
tags$p("The resulting figures summarise differences between two sets of estimates, they do not account for accuracy and other distributional properties.",
         "The default reference is estimates for a hypothetical farm with a single remnant patch that is occupied by Noisy Miners, and",
         "climate, rainfall, and woody vegetation canopy equal to the average of our training data.")
)
